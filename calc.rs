fn cubed(x: f64) -> f64 {

	x*x*x
}
fn squared(x: f64) -> f64{
	x*x
}
fn rooted(x: f64) -> f64{
// genununly do ts later ngl
}
fn pathg(mut x: f64; mut y: f64) -> f64{

	let hypotsq: f64;

	hypotsq = squared(x)+squared(y);

	let hypot = rooted(hypotsq);
	
	return hypot	
}
fn main(){


}
